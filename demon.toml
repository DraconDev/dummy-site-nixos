# Arcane Deployment Manifest
# This file tells Demon's GitOps Director how to deploy this project

[app]
name = "dummy-site"
type = "nix"

[deployment]
enabled = true
strategy = "rolling"
replicas = 1
target_group = "main1"

[[ingress]]
domain = "dummy.dracon.uk"
path = "/"
ssl = true

[build]
# For Nix flakes, we use the default output
dockerfile = ""  # Not used for Nix
context = "."
